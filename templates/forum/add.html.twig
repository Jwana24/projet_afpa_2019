{% extends 'base.html.twig' %}

{% block title %}{% trans %}Ajouter un post{% endtrans %}{% endblock %}

{% block body %}

    <main>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a class="btn-site" href="{{ path('posts_list') }}">{% trans %}Revenir à la liste des posts{% endtrans %}</a>
    </main>

{% endblock %}